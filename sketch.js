// var s = 0;
// var r = 0;
// var t = 0;
// var bDir = 1;
// var ry = 0
// var ty = 0

function setup() {
  createCanvas(500,500);
  background(255);
  // rectMode(CENTER);
  // angleMode(DEGREES);
  // push();
  //   translate(100,100);
    ellipse(width/2, height/2);
  // pop();

  // push();
  // rotateX();
  // rotateY();
  // rotateZ(0);
  // translate(-100, -100);
  // rect(width/2, height/2, 100, 100);
  // pop();
}

// function draw(){
  // translate(width/2,height/2);
  // background(0, 10);
  // s = s + 0.01;
  // t = t + 1;
  // r = r + 10;
  // if(t > 300) {
  //   bDir = 0;
  //   ry = random(0, height/10);
  //   ty = random(height);
  // }
  // if(t < -300) {
  //   bDir = 0;
  //   ry = random(0, height/10);
  //   ty = random(height);
  // }
  // if(bDir == 1) {
  //   t++;
  // } else {
  //   t--;
  // }
  // for(var i = 0; i < 100; i++) {
  //   s+=0.1;
  // }
  // // translate(t,ty);
  // // rotate(r);
  // // scale(s);
  // // // rotate(PI/(map(mouseX,0,400,TWO_PI, 1)));
  // // println(t);
  // rect(0, ry, 50, 50);
//
// }
